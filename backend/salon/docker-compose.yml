version: '3.8'

services:
  db_salao:
    image: mysql:8.0
    container_name: mysql-salao-leila
    restart: always
    environment:
      - MYSQL_DATABASE=${DB_NAME}
      - MYSQL_ROOT_PASSWORD=${DB_PASSWORD}
    ports:
      - "3306:3306"